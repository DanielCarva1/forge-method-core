{
  "timeoutMs": 600000,
  "checks": [
    { "name": "fmt",   "kind": "lint",      "command": "cargo fmt --all -- --check" },
    { "name": "check", "kind": "typecheck", "command": "cargo check --workspace" }
  ]
}
