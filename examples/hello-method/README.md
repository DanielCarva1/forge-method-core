# hello-method

Minimal Forge Method project example.

This folder proves the runtime can initialize durable project state without relying on chat history.

Try:

```powershell
python ..\..\skills\forge-method\scripts\forge_method_runtime.py status --root .
python ..\..\skills\forge-method\scripts\forge_method_runtime.py next --root .
```

