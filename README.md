# Forge Method Rust

Rust-only Forge Core for borrowed shells: Codex, Cursor, OpenCode, Claude, pi.dev, VS Code, and the future Forge app.

The current authority is `contracts/backlog/rust-only-core-backlog.yaml`.

`forensic-reference/` contains migrated reference material only. It is not runtime authority.
