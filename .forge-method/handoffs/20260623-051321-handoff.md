# Handoff

- created_at: 2026-06-23T05:13:21+00:00
- project: forge-method-core
- phase: 6-evolve
- status: input-resolved
- active_story: <none>

## Summary

PHASE A DONE + COMMITTED (391d99b): Forge v2.0 flock-coordination design spec locked and validated. Reality-evidence-gate (PLAUSIBLE->STRONG, STORM both-figures-verified), grill-gate (7/7 open questions resolved, Q2 TTL=30min+handoff-on-expiry, Q6 chronicle-rich), deep-research (grite arXiv:2606.19616 independently validates the architecture: 78%->0% dup, 3x throughput). 3 upgrades integrated into RFC v3: A=CRDT-hybrid (single-driver for integration FSM + CRDT projection for coordination state, Principle 18), B=completion-state (Principle 19, locks-alone-insufficient per grite), C=notify-don't-lock (Principle 20, CoAgent). Positioning: Forge sits ABOVE A2A and MCP (A2A-compatibility via opacity-vs-shared-state reconciliation). Design distributed: single-driver-across-machines via git-synced driver.lock. Human-human governance = explicit out-of-scope. Defect logged: evolve-reentry-routing-gap (guidance engine mis-routes new-demand-in-evolve to builder instead of evolve-project->discovery). **MAINTAINER CORRECTION 2026-06-23 (see `20260623-spec-correction-phase-b-poc-framing.md`):** the original Phase B framing ("use Forge AS-IS v1.34.1, no code changes, find gaps") only re-confirms `forge-runtime-audit.md`; you cannot validate v2 additions without implementing them. Phase B is now a **POC validating ALL 20 v2 design principles (1-20, not just additions 18/19/20)** — the maintainer's decision after POC is whether to commit to the entire v2 design and re-enter Phase 1 for it. Two complementary layers: (a) code substrate POC for state/coordination principles (agent_id, version field + optimistic concurrency, append-only handoffs, claim primitive, registry, CRDT projection); (b) behavior/facilitation POC for partner-experience principles (research affordance, grill-close, match-energy). Replicate grite metrics (dup-work rate, conflicting edits, throughput). Output gap-report.md with POC verdict (accept / iterate / reject). After accept, Forge Method re-routes evolve -> Phase 1 (discovery): interview -> PRD -> architecture -> build. The old "Phase C direct implementation" is now a candidate BACKLOG feeding that Phase 1 re-entry (closes evolve-reentry-routing-gap + honors RFC 6.5/Principle 12). NEXT DECISION NEEDED: pick the Phase B validation target (any project where 2+ agents run concurrently; mutant-run-horde-lab was only an example).

## Next Action

Phase B step 1: pick validation target + build minimal POC validating ALL 20 v2 design principles (two layers: code substrate for state/coordination principles + behavior/facilitation for partner-experience principles); run team-operating-model to declare driver/worker flocks, product-area-map for lanes, trunk-based-plan for commit-safety; stress-test concurrent agents THROUGH the POC; write gap-report.md with POC verdict (accept/iterate/reject). After accept verdict, Forge Method re-routes evolve -> Phase 1 (discovery): interview -> PRD -> architecture -> build. Blocked on maintainer choosing the validation target.
