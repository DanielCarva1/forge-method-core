# Evidence — v2-004: Fleet-mode append-only handoff/checkpoint

- kind: story-evidence
- story: v2-004
- created_at: 2026-06-23T08:30:00Z

## What was done

Added `is_fleet_mode(root)` helper (checks `agents/registry.yaml` presence) and `append_request(root, action, payload, agent_id)` helper (appends to `requests.ndjson`). Modified `cmd_handoff` and `cmd_checkpoint` to respect fleet mode: when `is_fleet_mode` is True and `--update-state` not explicitly set, handoff/checkpoint append to `requests.ndjson` instead of mutating `state.next_action`. Added `--update-state` / `--no-update-state` / `--agent-id` CLI flags to both commands.

## Verification

- `handoff --help` shows `--update-state`, `--no-update-state`, `--agent-id`
- `checkpoint --help` shows same flags
- py_compile: OK
- Backward compat: single-agent mode (no registry) = v1 behavior (mutates state)

Commit: b776edc
