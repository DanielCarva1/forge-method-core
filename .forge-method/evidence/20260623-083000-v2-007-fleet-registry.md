# Evidence — v2-007: Fleet registry support + is_fleet_mode detection

- kind: story-evidence
- story: v2-007
- created_at: 2026-06-23T08:30:00Z

## What was done

Added `is_fleet_mode(root: Path) -> bool` helper in `scripts/forge_method_runtime.py` that returns True when `.forge-method/agents/registry.yaml` exists. This is the fleet-mode detection primitive — presence of the registry file = opt-in to multi-agent behavior (append-only handoffs, driver-only state writes, lane claims). Also added `append_request` helper for worker→driver state-change requests.

## Verification

- `is_fleet_mode` correctly returns True when registry exists, False otherwise
- POC validation: the hot-take experiment used `agents/registry.yaml` successfully with 3 concurrent agents
- py_compile: OK

Commit: b776edc (same commit as v2-004 — both helpers added together)
