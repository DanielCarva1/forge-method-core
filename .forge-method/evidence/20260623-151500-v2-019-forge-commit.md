# Evidence — v2-019: Forge commit wrapper (GAP-1 fix — lane-aware staging)

- kind: story-evidence
- story: v2-019
- created_at: 2026-06-23T15:15:00Z

## What was done

Added `cmd_forge_commit` (CLI: `forge-commit`) to `scripts/forge_method_runtime.py`. In fleet mode, reads the caller's claimed lanes from `claims/*.lock`, reads lane→path mapping from `.forge-method/lane-paths.yaml` (flat YAML: `lane-id: path1, path2`), and `git add`s ONLY those paths before committing. Prevents the POC's `git add -A` cross-lane sweep. Falls back to `git add -A` when: single-agent mode (no registry), no claims, or no path mapping.

## Verification

- CLI: `forge-commit --help` shows `-m/--message` + `--agent-id`
- py_compile: OK
- Functional: in single-agent mode (no claims), stages all (v1 behavior preserved)
- In fleet mode with claims + lane-paths: stages only claimed paths

Commit: (this commit)
