# Evidence — v2-010: Lane claims with TTL + claim/unclaim/heartbeat commands

- kind: story-evidence
- story: v2-010
- created_at: 2026-06-23T15:15:00Z

## What was done

Added `cmd_claim`, `cmd_release` (CLI: `unclaim`), `cmd_heartbeat` to `scripts/forge_method_runtime.py`. Claims write `.forge-method/claims/<lane>.lock` (flat YAML with agent_id + expires). TTL default 30min. Claim checks existing lock: if unexpired and held by another agent → denied (exit 1). Heartbeat = re-claim (resets TTL). `_resolve_agent_id` resolves from `--agent-id` flag → `FORGE_AGENT_ID` env → `"default"`. `_is_claim_expired` checks the timestamp.

## Verification

- agent-a claims `test-lane` → "Claimed lane 'test-lane' (agent: agent-a, TTL: 5min)"
- agent-b claims same lane → DENIED: "Lane 'test-lane' is claimed by 'agent-a'. Try another lane or wait." exit code 1
- agent-a heartbeat → renewed (new expires timestamp)
- agent-a unclaim → "Released lane 'test-lane'."
- py_compile: OK
- Agent identity correctly resolved from FORGE_AGENT_ID env var

Commit: (this commit)
