# Adversarial Review — commit 102e193 (Frente A, R8 structural fix)

## Verdict: APPROVE_WITH_NITS

The type split is applied correctly at every site I traced (`ClaimContract.id: ClaimId`, `ClaimScope.id: ScopeId`, `AcquireRequest.scope_id: ScopeId`, `agent_id` correctly left as `StableId`, all `ClaimRejection` variants typed correctly — `scope_id: ScopeId` in `AlreadyClaimedByOther`, `claim_id: ClaimId` in `NotClaimant`/`ExpiredRequiresHandoff`/`IllegalTransition`, `ClaimExpiry.claim_id: ClaimId`, `BlockDetail.blocking_claim_id: ClaimId`). The engine still derives the canonical id via `ClaimId(format!("claim.{}.{}.{}", ...))` (claim_engine.rs:288) and `claim_holds_scope` compares `ScopeId == ScopeId` (claim_engine.rs:232). No mis-assignment found. `cargo build -p forge-core-cli --tests` is warning-clean; r8_property (3) + r8_binary_boundary (4) pass. Approving with doc/hygiene nits only.

## Bugs Found (severity: critical/major/minor)

- [minor] `crates/forge-core-cli/src/claim.rs:~84-95` — **Doc-comment corruption.** The `ClaimRef` enum's rustdoc block is contaminated with a leftover fragment of `run_acquire`'s docstring. Lines read `/// Run \`claim acquire\`. Loads existing claims...` / `/// # Errors` / `/// \`EnvConfig\` (5) if the claims dir is corrupt; \`RejectedByGate\` (2) if the` and *then* mid-sentence the `/// A claim reference as typed by an operator...` block continues. The `# Errors` heading and the `EnvConfig...` line are orphaned inside ClaimRef's doc, and `run_acquire` later (claim.rs:128) has its own separate (also slightly mangled — missing the `# Errors` header before `EnvConfig`) doc block. Fix: split the two doc blocks with a blank line and move the run_acquire fragment back to its function.
- [minor] `crates/forge-core-cli/src/claim.rs` `resolve_claim` docstring (lines h9W-xMw) — **Stale/misleading.** "The exact full-id match wins over the scope match so a scope with both a released and an active record stays unambiguous." With `ClaimRef` pattern matching, `Full` and `Scope` are mutually exclusive variants decided once at parse time — there is no "winning" between them anymore. The sentence describes the deleted slice-5 fallback. Rewrite to describe variant dispatch.
- [minor] `parse_claim_ref` (claim.rs:eyk) — **"parse, don't validate" incompleteness.** Empty string → `Scope(ScopeId(""))`; exact `"claim."` → `Full(ClaimId("claim."))`; neither can ever match a real record (resolves to None → "claim not found"), so not a functional bug today, but the function accepts unbounded garbage contrary to the stated King principle. More importantly: **nothing in `acquire`/`run_acquire` rejects a scope id that itself starts with `claim.`**, so an operator who types `--id claim.foo` at acquire time creates a claim whose scope.id `claim.foo` is then *misclassified as Full* on every subsequent release/heartbeat — silent lookup failure, the same R8 failure mode in a new costume. Low real-world probability (operators don't name scopes that way) but it is a genuine hole in the heuristic. Recommended fix: validate at acquire that `scope_id` does not start with `claim.`, or make the canonical prefix unforgeable (e.g. `claim#` or include the scope_kind in a way the operator token cannot reproduce).

## Nits

- `Cargo.toml` / `crates/forge-core-cli/Cargo.toml` — `tempfile` and `trycmd` added as workspace + dev-dependencies but **never imported** anywhere (`grep -r "use tempfile\|use trycmd\|tempfile::\|trycmd::" crates/` returns nothing). Dead dependency weight; the new tests deliberately use `std::env::temp_dir()` and repo-relative `target/` paths instead. Drop them or actually use them.
- `r8_property.rs:tmp_claims_dir` and `r8_binary_boundary.rs:fresh_bus` — temp/bus dirs are created but **never removed after the test**. Proptest's `AtomicU64` SEQ is process-scoped so `/tmp/forge-prop-r8-*` accumulates across runs; binary-boundary SEQ resets each process so it overwrites same-named dirs (effectively self-cleaning across runs) but leaves litter in `target/`. Harmless (gitignored) but worth a `Drop` guard or explicit cleanup.
- Pre-existing (not introduced here, carried from slice-5): `resolve_claim`'s Scope branch does `find(|c| c.scope.id == *id)` returning the first claim in filename-sorted order. If two claims share a scope-id string across *different* `scope_kind` (e.g. Story `s1` + Lane `s1`), the Scope token `s1` resolves non-deterministically to whichever file sorts first. Mitigated today because canonical filenames differ (`claim.story.s1.s1.yaml` vs `claim.lane.s1.s1.yaml`) so both can coexist on disk. Worth a TODO.
- `claim.rs` run_acquire region — the `# Errors` doc heading is missing immediately above `EnvConfig (5)...` for the real `run_acquire` (line hkY onwards), likely collateral from the same edit that scrambled the ClaimRef doc block.

## Test Quality Assessment

The tests **genuinely catch R8** and are not vacuous. `scope_id_strategy`'s regex `[a-zA-Z0-9_][a-zA-Z0-9_-]{0,18}` disallows dots, so a generated scope token can *never* equal the engine-derived canonical `claim.<kind>.<tok>.<tok>` — the `prop_assert_ne!(canonical, scope_id)` "vacuousness guard" therefore always holds and never turns failures into skips. The input is generated independently of `acquire`'s output, structurally breaking the circular oracle the commit message describes. The binary-boundary test spawns the real `forge-core` exe (via `CARGO_BIN_EXE_`) with operator-shaped argv (`--scope lane --id s1`) and asserts both scope-form and canonical-form release resolve, plus a negative test (`does-not-exist` must exit non-zero) so the lookup cannot degenerate to a no-op. Solid.

## Missing Coverage

- **No test for the `claim.`-prefix scope-id footgun** described above (acquire `--id claim.foo`, then release `--id claim.foo` → currently misparses to `Full` and fails to resolve). This is the one R8-shaped regression the new defenses do *not* pin.
- No test for cross-scope-kind scope-id collision (Story `s1` vs Lane `s1`).
- No test that `parse_claim_ref("")` / `parse_claim_ref("claim.")` produce a defined, rejected outcome rather than silently leaking through to "claim not found".
- `trycmd` was added presumably for snapshot/CLI tests but no `.trycmd`/`.md` cases ship — the binary-boundary coverage is hand-rolled with `assert_cmd` instead. Either add the trycmd snapshots or remove the dep.
