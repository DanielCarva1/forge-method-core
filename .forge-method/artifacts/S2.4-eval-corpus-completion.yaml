schema_version: "0.1"
artifact_kind: "story-completion"
story_id: "S2.4"
status: "done"
created_at: "2026-06-26"

intent: "Resolve grill R2: ship a router eval corpus + scorer so the C1 accuracy bar (>=90%) is measurable, not asserted."

delivered:
  module: "crates/forge-core-engine/src/eval.rs (EvalCase, EvalCorpusDocument, load_eval_corpus, score_router, RouterScore, corpus_coverage_gaps)"
  corpus: "contracts/eval/router-eval-corpus.yaml (42 cases spanning all 6 phases + anytime)"

design:
  no_model_in_core: "the engine ships NO router; score_router takes an injected closure Fn(&str)->Option<String>. An operator runs a host against the corpus and reports accuracy."
  accuracy_representation: "accuracy stored as basis points (u32, 0..=10000) to avoid floats in core; accuracy_percent() derived for human reporting"
  coverage_check: "corpus_coverage_gaps asserts every expected_workflow resolves in the 110-workflow catalog"

acceptance_criteria:
  - "corpus has >=40 cases (42) — PASS"
  - "spans all 6 phases — PASS"
  - "every expected id resolves in catalog — PASS (corpus_coverage_gaps empty)"
  - "scorer smoke test — PASS"

verification:
  cargo_test_engine: "PASS — 3 eval tests"
  cargo_clippy_pedantic_engine: "PASS — 0 warnings (fixed: # Errors doc, checked_div, u32::try_from, float tolerance, doc backticks)"
  cargo_test_workspace: "PASS — 199 tests, 0 failures"

open_question_OQ_R2: |
  The >=90% accuracy number is a RELEASE GATE checked by running the target host
  against this corpus, NOT a CI unit test (no LLM in core per AQ1/DC9). The
  corpus + scorer are the measurement infrastructure; the number is reported.

next_workflow: "slice 2 complete. Plan slice 3 (usability arc: CLI guide command + host interaction)."
