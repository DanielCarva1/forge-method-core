{
  "version": "2.0.4",
  "date": "2026-06-23",
  "title": "Bash option parser hotfix",
  "summary": "Forge v2.0.4 makes Bash verification scripts handle missing option values cleanly under `set -u` instead of crashing with `unbound variable`.",
  "highlights": [
    "`verify-fast.sh` now validates values for `--test`, `--match`, `--report`, `--junit`, `--workers`, and `--timeout`.",
    "`verify-all.sh` now validates values for `--report`, `--junit`, `--workers`, and `--timeout`.",
    "Missing option values now return exit code 2 with a clear `Missing value for --flag` message.",
    "Added Bash regression coverage for missing required option values."
  ],
  "full_notes_url": "https://github.com/DanielCarva1/forge-method-core/blob/main/release-notes/2.0.4.md"
}
