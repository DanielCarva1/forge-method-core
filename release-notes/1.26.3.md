# Forge Method 1.26.3

Forge Method 1.26.3 tightens startup so agents do not waste context or leak state-machine wording.

## Highlights

- Agents must route first and avoid broad project reads before `preflight`/`start`.
- Missing-state human output no longer prints `Project state: missing`.
- The skill forbids stale startup phrases such as "waiting for initialization details".
- Runtime output now frames missing-state folders as Forge setup choices.

## Notes

This is a patch release for startup behavior. It keeps the 1.26 Reality/Evidence Gate and 1.26.2 anti-cache contract.
