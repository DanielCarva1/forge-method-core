# Forge Method 1.26.4

Emergency reload support for stale Codex chat state.

## Highlights

- Added `reload` to the runtime as a read-only bootstrap contract.
- Added the `$forge-reload` skill as a tiny emergency entrypoint.
- Local installers now install both `forge-method` and `forge-reload`.
- Startup update checks now include `reload`.
