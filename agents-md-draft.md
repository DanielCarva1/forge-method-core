# AGENTS.md (DRAFT) — forge-method-core

> **DRAFT — not canonical.** Generated by Forge Method from project state.
> A human must review and approve this file before it becomes the canonical
> AGENTS.md (ETH Zurich C8 / Forge Principle 8 — human approval gate).

Target runtime: **any agent runtime**.

## Project

- name: forge-method-core
- phase: 4-build-verify
- status: in-progress
- module: runtime-builder
- autonomy_mode: autopilot
- commit_policy: off

## Brief

v2 PRD locked (prd-forge-v2.md). 26 stories across 9 epics (3 done, 23 planned). Entering build: spawn subagents on stories that touch different files (avoid same-file contention until GAP-1 forge-commit-wrapper lands).

## Lane map

_No `agents/registry.yaml` lanes defined. Run `product-area-map` to define Product Areas, owners, and parallel-work boundaries._

## Claim / handoff conventions

- State is file-backed: behavior that changes project progress must update `.forge-method/state.yaml`, `sprint.yaml`, story files, evidence, or `ledger.ndjson`.
- Multi-agent (fleet) mode is opt-in via `agents/registry.yaml`. When active, handoffs are append-only: workers emit requests to `requests.ndjson`; the driver applies them via `write_state`.
- Use `forge claim <lane>` / `forge unclaim <lane>` / `forge heartbeat <lane>` to coordinate. First-come, first-served with a TTL.
- Use `forge handoff --summary ...` to write a continuation handoff artifact; it does not mutate state in fleet mode.
- Optimistic concurrency: state writes carry `version`; a `VersionConflict` means re-read and retry (Principle 3/18/20).

## Decision gates

- Grill Gate: on record.
- Grill recommended at this decision-close point: no.
- Council / correct-course artifacts are durable; cite them in handoffs.

## Agent operating notes

- Run `forge next` / `forge resume` to load context and the recommended next workflow.
- Run `forge story list` to see open stories; claim a lane before editing shared files.
- Workflow docs stay compact state machines: `trigger`, `inputs`, `steps`, `outputs`, `done_when`, `blocked_when`, `handoff`.

---

_Generated by Forge Method `emit-agents-md`. Edit, then save as `AGENTS.md` once approved._
