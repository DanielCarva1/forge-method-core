# Product gap register

Status: current Rust source checkpoint `0.12.0` at the repository commit being
reviewed. This register records product gaps; it is not proof that a future tag,
asset, host integration, or field run exists.

## How to read this register

A gap appears here only when source tracing or direct public-interface evidence
showed that a promised user outcome is absent, incomplete, or still lacks the
external evidence needed to claim it. Internal primitives, a public reachable
surface, a shipped release asset, and a real-host result are different facts.

Closed status vocabulary:

- `implementation_absent`: the required product behavior is not implemented;
- `partial_public_surface`: useful primitives or expert interfaces exist, but the
  ordinary supported journey is not complete;
- `external_evidence_pending`: source may contain the control, but only a release,
  hosted run, or field review can close the claim;
- `engineering_hardening_debt`: a specific implementation or release-control
  weakness remains before the affected claim is safe.

Priority is outcome-based: `P0` blocks or can invalidate the first supported
production journey; `P1` blocks the complete operated-product lifecycle; `P2`
blocks ecosystem breadth but not the first core journey.

## Confirmed gaps

| ID | Priority | Classification | Real gap | Current evidence and boundary | Closure evidence |
|---|---:|---|---|---|---|
| **GAP-001** | P0 | `partial_public_surface` | **Ordinary first-use authority bootstrap is not self-contained.** A clean project reaches an action that requires a human-origin broker. Forge ships the closed external-origin event protocol, public-key registry, verification, and `workflow action apply` path, plus a deliberately lower-authority local cooperative signer. It does not ship a host-owned authenticator/signer and bridge that can attest a chat event outside agent control. | `workflow next` can report missing principal/broker registries and require `human_approval_broker`. `workflow action authorize` correctly rejects that authority class. `skill/start-forge/SKILL.md` delegates creation of the signed origin event to the host. This security boundary must not be weakened by letting the agent mint human authority or invoke a signer as an oracle. | From one ordinary chat message, a selected host integration authenticates a native human interaction, keeps private authority and signer invocation outside agent reach, binds opaque host-event provenance, and completes the public action path. Adversarial tests prove the agent alone cannot manufacture or cause approval. |
| **GAP-002** | P0 | `engineering_hardening_debt` | **C2.2 complete-state backup/verify/restore and durable reinitialize-as-new are source-complete; runtime interruption and recovery evidence remain open.** | Public `forge-core backup create`, `forge-core backup verify`, `forge-core restore preflight`, and `forge-core restore apply` source binds the Project Link, complete sidecar authority set, validated public external registries/anchors, exact release identity, immutable archive/receipt publication, project-bound restore completion, and fail-closed restore paths. Public `forge-core project reinitialize plan/apply` source now captures exact predecessor and diagnosis, seals one plan-derived confirmation, retains the predecessor through a Store-owned lifetime anchor, reserves the unrelated destination, applies exact successor CAS, persists operation-scoped plan/WAL/receipt leaves, validates retry receipts against the live successor, and never reinstalls abandoned authority. Candidate documents remain non-authoritative, `selected_host` remains none, and external broker private keys remain excluded and owner-managed. Targeted locked all-target compile-only checks passed for contracts, Store, command surface, validation, and CLI; runtime, interruption/retry, mixed-version, failure-injection, MSRV/platform, hosted-CI, release, and field evidence did not run. | Execute the deferred Rust tests plus interruption/retry, stale-diagnosis, cross-project substitution, identity/location collision, mixed-version, and failure-injection campaigns. Preserve the explicit restore/reinitialize authority split and do not promote compilation into operational, release, or field evidence. |
| **GAP-003** | P0 | `implementation_absent` | **No concrete reference-host adoption package is shipped.** | The generic host manifest/projection recognizes Codex, Claude, Cursor, and OpenCode, and MCP readiness can generate a client configuration. Those are adapter-neutral primitives. The release payload contains no named-host plugin/extension installer or host conformance suite. Default MCP is intentionally read-only and is not a substitute for the separate governed workflow CLI/authority path. | One selected host passes a source-level clean-install, setup, chat-only authority, conflict, and replacement-session conformance suite; GAP-006 separately requires that exact integration to ship and pass production-host evidence. |
| **GAP-004** | P0 | `engineering_hardening_debt` | **C2.3 product-lifecycle and C2.4 resilience-corpus source are implemented, while execution and external evidence remain open.** | Public `forge-core lifecycle setup/status/doctor/install/update/rollback/uninstall` source owns one explicit marked install root outside consumer projects and sidecars. Closed typed release bundles, existing distribution admission and artifact verification, bounded no-follow byte reads, exact SHA-256, semantic compatibility, Store-owned immutable generations and generation-local receipts, exact retry, downgrade/content-drift rejection, verified rollback, typed release notes, exact host-config status, and product-owned-only uninstall are implemented. The source corpus covers partial setup, stale host config, interrupted update, downgrade, wrapper mismatch, retained-state uninstall, backup interruption, and replacement-machine restore. Unknown/modified files, projects, sidecars, operator anchors, backups, registries, and external keys are preserved; `selected_host` remains none. Targeted locked all-target compilation passed; Rust tests, runtime interruption/mixed-version execution, failure injection, MSRV/platform, hosted CI, publication/release, downloaded-asset, real-host, and field evidence did not run. | Execute the deferred C2.4 corpus and wider failure-injection, platform/MSRV, hosted-CI, release/downloaded-asset, real-host, and field evidence without broadening installation custody. |
| **GAP-005** | P0 | `external_evidence_pending` | **Release controls are source-complete; platform and release evidence remain open.** | Release source uses locked Cargo resolution and a declared Rust 1.85.1 lane, has a Linux/aarch64 native `ubuntu-24.04-arm` packaged install-smoke job, uses a portable bounded wrapper resolution, and builds a deterministic checksummed release-set manifest binding canonical ordered archives, source identity, and the exact-version SBOM. No workflow run, archive verification, publication, or downloaded-asset evidence is claimed. | Run the deferred native ARM64, release-set, archive, hosted-CI, publication, and downloaded-asset verification evidence at C3.2-C3.3. |
| **GAP-006** | P0 | `external_evidence_pending` | **There is no verified public `0.12.0` release and no production-host P7F result in the evidence reviewed here.** | Source version is `0.12.0`, but no matching tag was found during the audit; `v0.4.0` is only a historical Rust predecessor. Source workflow and P7F bundle checks prove control structure, not publication, host behavior, semantics, or actor independence. | A matching immutable tag run publishes the exact assets and sidecars; independent verification checks identity and installation; one supported production host produces and receives independent review of the bounded P7F journey; consecutive hosted CI timing evidence closes P7G. |
| **GAP-007** | P1 | `external_evidence_pending` | **C5.1-C5.3 post-BuildVerify continuity is source-complete, while runtime, failure-injection, hosted, release, and field evidence remain open.** | C5.1 defines candidate-only generation-chained episode contracts and pure decisions. C5.2 binds guarded admission to the exact snapshot, ledger head, state version, phase, active release, predecessor, admitted policy/assurance, and hard transition gate; automatic planning still stops at BuildVerify. C5.3 persists complete snapshots in ledger epoch `0.8`, retains historical summary-only `0.7` readability, and reconstructs the latest exact release, rollback baseline, observations, intake, evolution identity, and next action from a fresh process. Owner-bound request transitions, exact stable request/claim IDs, typed dependencies, evidence-only mutation handoffs, proof/lease-bound completions, reviewed health recovery, deterministic retry, and claim-WAL liveness all fail closed. The replacement projection is cloned data only: it grants no claim, mutation, phase, release, signing, private-key, trust, lifecycle, protected-anchor, install, activation, or host-selection authority. `selected_host` remains none. Targeted workflow-governance TCB and kernel all-target compilation passed; Rust tests and broader evidence did not run. | Execute deferred runtime fresh-process and repeated-episode tests, interruption and failure injection, stress/fuzz and MSRV/platform matrices, hosted CI, publication/release verification, and real-host/field review without weakening candidate/authority boundaries. |
| **GAP-008** | P2 | `partial_public_surface` | **C6.2 immutable remote Domain Pack acquisition is source-complete at the candidate-byte boundary, while transport and deferred evidence remain incomplete.** | C6.1 derives first acquisition plus initialized-project install, upgrade, rollback, remove, and changed-Core rebase without human-authored lifecycle documents. C6.2 now implements signed mirrors and immutable descriptors, publisher/registry signature coverage, monotonic protected anchors with cumulative revocation, candidate-only online/cache/offline planning, exact-byte verification, deterministic bounded cache policy, rollback provenance, and a local CLI adapter that imports an exact operator-provisioned object set. The CLI intentionally performs no DNS, TLS, redirects, subprocess download, or general network transport and grants no trust, install, lifecycle, or activation authority. Targeted compile-only checks passed; no runtime/heavy tests, failure injection, hosted CI, release/publication, independent semantic review, real-host, or field evidence ran. | A selected transport owner may later execute the already-derived signed network attempts without weakening the authority split. Deferred C3.2-C3.4 runtime/stabilization, failure-injection, hosted-CI, release/publication, independent semantic-review, real-host, and field evidence validate any supported public claim. |
| **GAP-009** | P2 | `partial_public_surface` | **C7.1-C7.2 provide a candidate-only external-author workflow, but authoritative signing, admission, publication, and external evidence remain incomplete.** | C7.1 provides a deterministic generic skeleton, exact sealed-Core validation, a unified candidate-only author test report, and a non-authoritative CLI adapter. C7.2 now composes that evidence with C6.2 immutable descriptors for package preparation, independent-review readiness, exact external-signing requests, unverified detached-signature evidence assessment, unsigned catalog evolution, and cumulative revocation proposals. Existing authority owners still exclusively own signing bytes, private keys, cryptographic verification, policy, catalog admission, protected-anchor advancement, and revocation enforcement. C7.2 adds no publication transport, CLI, trust, lifecycle commit, install, or activation authority; actual signing, independent review, publication, runtime, and field evidence remain pending. | External authors complete the full SDK lifecycle without domain-specific core Rust, while authority-owned signing and catalog admission, bounded publication transport, independent review, hostile/revoked/incompatible runtime tests, and supported field evidence fail closed and preserve the candidate/authority split. |
| **GAP-010** | P1 | `implementation_absent` | **Named-host support breadth is not implemented or certified.** | Recognizing target enum values does not establish installability, authenticated human-origin events, governed mutation, or field support for Codex, Claude, Cursor, and OpenCode. | After the reference integration freezes the contract, each intended target passes the same versioned adapter suite or is explicitly marked unsupported with the failed capability named. |

## Explicit non-gaps and non-solutions

- The Rust/YAML product is authoritative. The unrelated legacy Python/plugin
  history is forensic input only and must not be restored or copied as runtime.
- Manual source installation and verified-archive installation exist; the gap is
  the owned lifecycle and host setup around them, not the ability to build a CLI.
- Generic host manifests, read-only MCP, hardened MCP readiness, action packets,
  broker registries, claims, handoffs, worktree-isolation proposals, readiness
  policies, and Domain Pack lifecycle operations exist. They must be reused, not
  reimplemented under new names.
- Read-only MCP is a deliberate authority boundary. Closing host adoption must
  not expose mutating workflow authority through a transport merely for
  convenience.
- Same-OS-principal hostile isolation and exhaustive discovery of every unknown
  unknown remain disclosed boundaries, not silently promoted promises.

## Ordering rule

The canonical implementation sequence is
[`contracts/plan/product-gap-closure-plan.yaml`](../contracts/plan/product-gap-closure-plan.yaml).
Work starts with **GAP-001** because an ordinary user currently cannot cross the
first required human-authority boundary. Any implementation must preserve that
boundary rather than replacing it with agent-controlled signing. GAP-002 and
GAP-005 are also release blockers and must close before publishing the resulting
supported journey.
