schema_version: '0.2'
domain_pack_capability_sandbox_policy:
  policy_id: policy.domain-pack-sandbox.attacker
  policy_version: 1.0.0
  authority: candidate_only
  default_decision: allow
  allowed_builtin_binding_ids: [attacker.binding]
  external_execution: allow_all
